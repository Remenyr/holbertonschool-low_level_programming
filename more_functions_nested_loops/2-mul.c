#include "main.h"
#include <stdio.h>

/**
 * mul - check of the proyect
 * @a: is a Variabel
 * @b: is another variable
 * Return: 0
 */

int mul(int a, int b)
{
	int pro;
	
	pro = a * b;
	return (pro);
}
